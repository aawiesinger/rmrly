%table#matches
  %thead
    %tr
      %th Profile Picture
      %th Name
      %th Profile
      %th Chat
  %tbody
    - @matches.each do |user|
      %tr
        %td= gravatar_for user
        %td= user.name
        %td= link_to "View Profile"
        %td= link_to "Chat", chat_path(@logged_in_user,user)
