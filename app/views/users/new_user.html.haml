%h1 Sign Up

= form_tag users_path do

    = label :user, :email, 'Email'
    = text_field :user, 'email'
    
    = label :user, :password, 'Password'
    = text_field :user, 'password'
    
    -# not sure if :post is correct
    = submit_tag 'Sign Up', :post => users_path